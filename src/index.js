import {Actions, diffNode} from './diff'
import element from './element'
import string from './string'
import dom from './dom'

const diff = {
  Actions,
  diffNode
}

export {
  element,
  string,
  diff,
  dom
}
