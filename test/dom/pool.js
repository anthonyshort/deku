import assert from 'assert'
import {component,dom,scene} from '../../'
import {mount} from '../helpers'

it('should pool dom nodes', function(){

})

it('should pool dom nodes when there are text nodes in between', function(){

})

it('should empty the pools when the renderer is unmounted', function(){

})